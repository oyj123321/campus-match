# Changelog

All notable changes to CampusMatch.

---

## [1.3.0] — 2026-07-28

### 问题（上次）
- 竞品调研显示：用户更在意「匹配理由 / 仪式感 / 配上后怎么开口」，而不是算法题量
- 我们结果页只有短标签，缺少相处说明书与破冰；也缺少「预约本周 → 周二揭晓」仪式

### 改动
- **Added / 相处说明书**：`summary` + 更完整共同点/差异文案写入 insight
- **Added / 破冰话题**：基于共同兴趣与相近量表题自动生成 3 条；页面与邮件展示
- **Added / 本周预约**：`opt_in_week` + `POST/DELETE /api/match/opt-in`；倒计时到揭晓
- **Added / 揭晓模式配置**：`REVEAL_REQUIRE_OPT_IN`（批量只匹配已预约）、`INSTANT_MATCH_ENABLED`（冷启动提前揭晓）
- **Improved**：旧配对加载时自动补全说明书与破冰并回写

---

## [1.2.3] — 2026-07-28

### 竞品深度调研

全面调研 CampusDate (交大)、MatchUs (浙大)、FDU Date (复旦)、Solmate (复旦)、哈啰 Match 的产品、问卷、算法、商业模式和用户反馈。

#### 关键发现
- **算法精度不是护城河**：65题 vs 15维 → 最终恋爱转化率都是 ~6%
- **核心产品价值** = 降低社交启动成本 + 制造仪式感 + 提供"被看见"的体验
- **匹配理由是产品的灵魂** — 竞品用户最高频好评词
- **固定揭晓时间 > 随时可刷** — 每周二晚9点是竞品共同选择
- **MatchUs ¥19.9/次是唯一跑通的付费模型**，但仍是亏本运营

#### 改进方向
- P0: 匹配理由展示、破冰话题、学校认证徽章
- P1: 扩至50题、每周二揭晓页面、跨校匹配
- P2: 匹配后反馈、分享裂变、AI红娘

#### 差异化定位
**大湾区唯一跨境校园恋爱匹配平台** — 竞品都在内地高校，港澳是空白。

#### 文档
- `docs/competitor-research.md`：完整竞品调研报告（问卷设计、商业模式、用户反馈、扩展路径）

---

## [1.2.2] — 2026-07-28

### 问题（上次）
- 一对一配对成功后，页面「历史」仍展示旧 Top-5 留下的其他人姓名与微信号，隐私与产品逻辑都不对

### 改动
- **Fixed**：`Match.active` 标记；新配对会把该用户其余配对降为失效
- **Fixed**：`/api/matches` 默认只返回有效配对；失效记录不返回微信号
- **Migration**：启动时清理每人多条有效配对，只留最近/最高分的一条
- **UI**：按钮改为「当前配对」，文案明确只展示一人

---

## [1.2.1] — 2026-07-28

### 问题（上次）
- 默认 `realtime` Top-5，点一次匹配会出好几个对象，和「一对一」产品预期不符
- 匹配邮件：失败仍标记 `notified=true`，且「更新已有配对」不发信，导致真实用户收不到通知
- 开发者难以理解余弦 / 匈牙利各自用在什么场景

### 改动
- **Fixed / 默认一对一**：`MATCH_MODE=one_to_one`，点匹配只返回得分最高的 1 人；每周额度默认改为 1
- **Fixed / 邮件通知**：每次成功配对会尝试给你发信；只有发到你邮箱成功才标 `notified`；页面展示「发给你/对方」成功或失败原因
- **Added / 匹配说明**：匹配页可展开「匹配是怎么算的」通俗说明（含匈牙利一句话解释）
- **Note**：演示账号如 `alice@um.edu.mo` 不是真实邮箱，发给对方常失败属正常；你自己的学校邮箱应能收到

---

## [1.2.0] — 2026-07-28

### 问题（上次）
- 可无限点「开始匹配」，缺少运营节奏（额度/冷却）
- 批量匹配只是配置项，没有可执行的周二任务与手动触发入口
- 首页看不到各校人数；历史页差异信息不足；邮件失败时用户容易以为「没结果」

### 改动
- **Added / 运营额度**：每用户每周新建匹配上限（默认 3）+ 冷却（默认 12h）；`/api/match/status` 供页面展示
- **Added / 批量任务**：`batch_job.py`（`--now` / `--schedule`）、可选进程内调度、`POST /api/admin/batch-run`
- **Improved / 匹配页**：额度与下次批量时间、共同点+差异、邮件失败仍展示结果提示
- **Improved / 首页**：各校已验证人数 + 总人数 + 批量匹配时间说明
- **Docs**：更新 `docs/mvp-roadmap.md`、`.env.example`
- **Chore**：清理 `app.py` 未使用导入

---

## [1.1.0] — 2026-07-28

### 问题（上次）
- 匹配写死「男只找女 / 女只找男」，无法支持同性或双性取向
- 上线前缺少生产开关、验证码限流、隐私说明与健康检查，不敢直接给同学用

### 改动
- **Added / 择偶取向**：问卷基本信息增加「希望匹配的性别」（男 / 女 / 男女都可以）；匹配需双方取向互相接受
- **Fixed / 匹配引擎**：`orientation_compatible` 双向过滤；批量模式在含同性/不限时改用贪心配对
- **Added / MVP 1.1 上线项**：`FLASK_DEBUG`、Session 安全、注册限流、`/api/health`、首页隐私说明
- **Fixed / 注册**：SMTP 失败时仍返回验证码到页面，不阻断验证流程
- **Docs**：`docs/mvp-roadmap.md`；`.env.example` 补充生产配置项
- **Seed**：默认异性取向；Emma 设为 `both` 便于演示

---

## [1.0.1] — 2026-07-28

### 问题（上次）
- 问卷量表滑块默认停在 3，进度却显示未答/提交又偷偷带上默认值，交互混乱
- `answers` JSON 键是字符串、匹配逻辑用 int 取，导致一票否决和匹配理由实际失效
- 「重新匹配」遇到已有记录直接跳过，页面像匹配失败；旧逻辑还可能重复发邮件
- 种子用户兴趣选项写错（咖啡馆/民谣等），特征向量对不上题库

### 改动
- **Fixed / 问卷页**：改为 1–5 点选；未点不算已答；提交前校验并高亮缺题
- **Fixed / 数据层**：`answers`、`important_qids` 读写统一为 int 键
- **Fixed / 匹配**：更新已有 Match 并返回结果；邮件只通知新匹配
- **Fixed / 种子与 API**：选项纠错 + `seed.py --refresh`；不足 20 题拒绝提交；过滤非法 multi
- **Improved**：进度 sticky、已答/未答状态、★ 重要标记、移动端 Likert；共同点含兴趣交集
- **Docs**：约定每次推送前必须更新本更改日志（见 `.cursor/rules/changelog-on-push.mdc`）

---

## [1.0.0] — 2026-07-27

### MVP 首个完整可用版本

#### Added
- **学校邮箱注册 + QQ SMTP 真实邮件验证**
  - 域名白名单自动识别学校
  - QQ 邮箱 SMTP 发送 6 位验证码
  - 10 分钟过期机制
  - 开发模式 fallback：邮件失败时控制台打印 token

- **32题深度问卷系统**
  - 四维度：核心价值观(8) / 生活习惯(8) / 情感风格(8) / 兴趣爱好(8)
  - Scale 滑块 (1-5) + Multi 多选
  - "对我很重要" 标记（权重 ×2）
  - 一票否决（出轨/孩子/婚姻/抽烟）

- **81维特征向量 + 余弦相似度匹配**
  - 问卷答案 → 归一化特征向量
  - 余弦相似度 Top-N 实时匹配
  - 匈牙利算法全局最优匹配（批量模式备用）
  - 一票否决过滤

- **匹配结果邮件通知**
  - 双方收到对方微信号 + 匹配度百分比
  - 共同点 + 差异分析
  - 样式化 HTML 邮件

- **种子数据生成器**
  - 澳大 8人（4男4女）+ 澳科 1人
  - 差异化人设：心理学/工科/法律/商科/传理/教育/金融/计算机/设计
  - 81维特征向量预计算

- **公网部署**
  - serveo SSH 反向隧道，零成本
  - pythonw 后台运行，不依赖终端
  - .env 环境变量管理

- **爬虫策略模块**
  - 多源搜索策略生成
  - 学校兴趣标签自动提取
  - 日报系统同款 pipeline

#### Supported Schools
- 澳门大学 (um.edu.mo)
- 澳门科技大学 (must.edu.mo)
- 澳门理工大学 (mpu.edu.mo)
- 澳门旅游大学 (iftm.edu.mo)
- 澳门城市大学 (cityu.edu.mo)
- 上海交通大学 (sjtu.edu.cn) — 参考实现
- 复旦大学 (fudan.edu.cn)
- 同济大学 (tongji.edu.cn)
- 华东师范大学 (ecnu.edu.cn)

#### Tech Stack
- Python 3 / Flask / SQLAlchemy / SQLite
- Jinja2 / Vanilla JS / CSS
- QQ SMTP / serveo SSH tunnel

---

## [0.1.0] — 2026-07-27 (early)

### 初始原型

- Flask 骨架 + SQLite
- 标签关键词匹配（简单交集）
- 上海高校邮箱白名单
- 开发模式邮件打印

---

### 版本命名规则

```
v<MAJOR>.<MINOR>.<PATCH>

MAJOR: 不兼容的架构变更
MINOR: 新功能、新学校、新算法
PATCH: Bug 修复、样式调整
```
