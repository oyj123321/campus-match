# Changelog

All notable changes to CampusMatch.

---

## [1.9.4] — 2026-07-29

### 问题（上次）
- 大范围公测条件未成文，易误判「已经可以广而告之」
- 缺少「先本机测再上正式云」的发布纪律；移动端问题未列入优先事项

### 改动
- **Added / 硬门槛**：`docs/beta-launch.md` 顶部记录大范围公测 7 项硬门槛（邮件隔离、域名 HTTPS、备案、发信、删号、移动端、禁止直推正式站）
- **Added / 发布纪律**：本机自测（含内测号）→ push → 云上 pull 重启；写入 beta-launch 与 ops-maintenance
- **Improved / 路线图**：`docs/mvp-roadmap.md` 将移动端与邮件投递标为 P0

---

## [1.9.3] — 2026-07-29

### 问题（上次）
- 内测账号仍需真邮件验证码，假学校邮箱收不到信，自测成本高

### 改动
- **Improved / 内测号免验证**：本地部分为 `cmtest`/`beta`/`test` + 数字（如 `cmtest01@um.edu.mo`）注册后直接登录进问卷；验证码可空或随便填
- **Changed / 前端**：收到 `beta_skip_verify` 时跳过验证码步骤

---

## [1.9.2] — 2026-07-29

### 问题（上次）
- 云服务器上线后，SQLite 库只在云机本地，缺少「定时拉回本机」的备份办法与说明

### 改动
- **Added / 本机拉库备份**：`scripts/backup-from-cloud.ps1`（scp 拉取云上 `campus_match.db` 到 `D:\backup\campus-match\`，保留约 60 天）
- **Added / 说明**：`scripts/README-backup.md`（SSH 免密、Windows 计划任务每 3 天下午 15:30、还原步骤）

---

## [1.9.1] — 2026-07-29

### 问题（上次）
- 破冰话题改用"军师支招"风格后，文案 AI 味反而更重，用户反馈不自然
- `needs_regen` 判断逻辑在改回字符串格式后未同步更新，导致每次请求都重新生成并覆盖数据库，进入无限重算循环

### 改动
- **Reverted / `questionnaire.py`**：破冰话题完全恢复原始模板风格——`你们对「xxx」看法几乎一样——可以聊聊：平时遇到这种情况你会怎么做？`；敏感 QID 5/6/8/13/24 仍然排除
- **Fixed / `app.py`**：`needs_regen` 改为仅在 `icebreakers` 为空时重算，去掉错误的字典格式检测条件，修复无限重写循环
- **Changed / `static/i18n.js`**：破冰标题改回"破冰话题"，去掉"可复制发送"提示文案
- **Changed / `email_service.py`**：邮件中破冰标题同步改回"破冰话题"

---

## [1.9.0] — 2026-07-29

### 问题（上次）
- 匹配成功后双方没有可靠的联系渠道——微信号既非所有人都有，也不该强制填写
- 破冰话题生硬，像在念问卷报告，且无法直接复制发出
- 破冰/契合点中会出现婚姻/孩子/出轨/吸烟/前任等敏感话题，容易尬场
- 破冰标题带有"（宿舍卧谈版）"字样，不够正式
- 问卷缺少让用户向对方自我介绍的出口

### 改动

#### 联系方式重构
- **Changed / `models.py` `batch_job.py` `app.py`**：`wechat_id` 改为纯可选字段；匹配成功后自动互换双方学校邮箱（注册时已验证），无需额外填写
- **Changed / `templates/profile.html` `static/i18n.js`**：联系方式输入框改为"附加联系方式（可选）"，示例格式 `wechat:mahuateng1998`
- **Changed / `templates/matches.html`**：匹配卡片新增学校邮箱展示行，附加联系方式若有则显示
- **Changed / `email_service.py`**：匹配通知邮件表格中增加学校邮箱列，附加联系方式单独一列（无填写时显示"—"）
- **Changed / `app.py`**：移除"必须填微信才能参与匹配"的校验；`serialize_match_payload` 在有效配对时返回 `email` 字段

#### 问卷自由留言（Q40）
- **Added / `questionnaire.py`**：新增 Q40，类型 `text`，可选，最长 2000 字；不进入特征向量，不影响匹配分数
- **Added / `questionnaire.py`**：`get_open_letter()` 函数用于提取留言内容
- **Added / `templates/questionnaire.html`**：末尾新增 textarea 输入框，附说明"真诚就是必杀技"
- **Added / `templates/matches.html` `email_service.py`**：配对结果页与邮件中展示"TA 留给你的话"

#### 军师支招（破冰重构）
- **Improved / `questionnaire.py`**：`get_compatibility_insight` 完全重写；兴趣题优先生成破冰，每条含 `tip`（军师耳语）和 `send`（可直接发出的文本）
- **Fixed / 敏感话题**：QID 5/6/8/13/24（婚姻/孩子/出轨/吸烟/前任）从契合点、差异列表、破冰建议三处展示层全部移除；仍参与内部算法打分
- **Fixed / 标题**：去掉"（宿舍卧谈版）"，统一为"军师支招"
- **Added / `templates/matches.html` `static/style.css`**：每条破冰建议新增"复制"按钮，可一键复制可发送的开场白
- **Added / `static/i18n.js`**：新增 `card.ice.send` `card.ice.copy` `card.ice.copied` 等四语言翻译 key

#### 其他
- **Improved / `app.py`**：问卷提交时正确跳过 `text` 类型题的「重要」加权；必答题数量计算排除可选题
- **Improved / `templates/index.html`**：首页文案更新，说明学校邮箱自动互换机制

---

## [1.8.2] — 2026-07-29

### 问题（上次）
- 上线后缺少「日常怎么养站、出事怎么办、如何看库里每个人数据」的成文说明

### 改动
- **Added / 运维手册**：`docs/ops-maintenance.md`（备份习惯、systemd/日志、常见故障表、SQLite 图形界面与 SQL/Python 查人、删号示例、安全底线）
- **Improved / README**：文档索引增加运维手册链接

---

## [1.8.1] — 2026-07-29

### 问题（上次）
- 上云 / 公开访问步骤散落在对话里，买腾讯云轻量机后缺少可复用的部署文档
- 给其他学校定制时，运维与换校要点未集中记录

### 改动
- **Added / 腾讯云部署指南**：`docs/deploy-tencent-lighthouse.md`（68 元档上海轻量机：SSH、`.env`、systemd、Nginx、备份、域名/备案说明）
- **Improved / 公测清单**：`docs/beta-launch.md` 增加「本周最小上线」并链到部署文档；文案与不展示分数等现状对齐
- **Added / 换校复用说明**：部署文档第 10 节（域名白名单、独立库、重跑部署）

---

## [1.8.0] — 2026-07-28

### 问题（上次）
- 额度只约束发起方：热门用户会被多人反复「抢走」配对，一周内可能收到大量匹配通知
- 匹配结果对用户展示契合度百分比；最高分也可能很低，或反而造成分数焦虑，不利于自然开聊

### 改动
- **Fixed / 双向周额度**：本自然周内，任何人作为发起或被配任一方，新建匹配默认最多 1 次；候选池与落库均排除本周已配满的人，禁止顶掉对方本周已有配对（`batch_job` / `match_pool` / `/api/match`）
- **Improved / 不展示匹配度**：API、匹配页、邮件均不再返回/显示分数与进度条；只展示若干契合点、破冰话题与微信；相处说明书文案也不再写百分比
- **Changed / 内部门槛**：`MATCH_MIN_SCORE` 默认 **15%**（仅用于内部排序过滤，不对用户展示）；`.env.example` / 文案同步

---

## [1.7.0] — 2026-07-28

### 问题（上次）
- 问卷存在重复题（宠物两问）、定居题把「老家/城市等级/海外」混在同一量表轴上
- 部分端点含糊或易引发误解（婚姻「可有可无」作右端、社会议题「开放进步」、吸烟表述生硬等）
- 量表向量把选 1 编码为 0，导致两人答案完全相同（都选左端）时相似度反而为 0
- 后端只要求答满 20 题即可完成；多选允许「不玩游戏」与具体游戏类型同选
- 「一票否决」规则对用户不透明，且异地恋作硬否决易误伤；婚姻/孩子中间态与明确对立需区分

### 改动
- **Removed / 重复宠物题**：删除原「想养宠物吗」，保留并改写「对宠物的态度」为未来是否愿意和伴侣养宠物；题号连续为 1–39
- **Improved / 定居题**：改为可多选（一线/二线/三四线/小县城/海外/暂不确定），海外不再排在一线「更右边」
- **Improved / 题干与端点**：婚姻改为「必须结婚 ↔ 完全不接受」；社会议题改为「传统 ↔ 开放」；吸烟改为「你对吸烟的态度」；并修缮表达爱意（改多选）、约会账单、冲突沟通、吃醋→情绪支持、约会形式等重叠/含糊题
- **Fixed / 特征向量**：量表改为左右互补双维编码，消除右端偏置；向量现为 126 维
- **Fixed / 完成度**：前后端均要求答完全部 39 题；`questionnaire_completed()` 按题型逐题校验
- **Fixed / 多选互斥**：「不玩/不运动/不太看书/什么都听/暂不确定」等与其它选项互斥（前端+后端）
- **Improved / 一票否决**：婚姻/孩子在明确相反意愿时否决；出轨观、吸烟态度差 ≥3 否决；异地恋降为普通题；问卷页增加说明文案（四语）
- **Added / 内测号**：`beta01`–`beta10` 统一验证码 `BETA01`（不写库，避免 unique 冲突）
- **Improved**：首页/README 题数改为 39；种子数据与演示账号刷新

---

## [1.6.0] — 2026-07-28

### 问题（上次）
- 大湾区天际线缩在页面底部一小条，地标认不出来、毫无存在感
- 全站只有简体中文，澳门用户群（繁体/英语/葡语）无法切换语言
- 匹配页「已预约/已开启」按钮状态靠中文文本匹配判断，多语言下会失效（顺带发现的隐患）
- 页面内脚本先于页尾的 `api()`/i18n 脚本执行，存在引用顺序隐患

### 改动
- **Improved / 地标主视觉**：地标 SVG 移到上半屏（fixed 顶部、按比例自适应、底部渐隐），以澳门为主放大重绘——澳门塔（观景舱+天线）、新葡京（莲花火炬塔+蛋形裙楼）、大三巴（五层立面带门窗洞+十字架）、澳门科学馆，香港 IFC/中银/ICC 作次要点缀
- **Added / 下侧海面**：底部改为双层漂移海浪动画 + 港珠澳大桥剪影，填充下半侧
- **Added / 四语言支持**：新增 `static/i18n.js`（简体/繁體/English/Português 词典 + `t()/tf()` 引擎），右上角下拉切换、localStorage 记忆；覆盖首页/验证页/问卷页/匹配页全部界面文案与动态字符串
- **Added / 问卷题目翻译**：`questionnaire.py` 新增 `QUESTION_I18N`，40 题题干/量表两端/多选项均有繁英葡翻译；多选项仅翻译展示、提交仍存简体原值，不影响特征向量
- **Added / 学校名与揭晓时间翻译**：学校名按语言显示（如 University of Macau），「每周二 21:00」的星期自动转换
- **Fixed / 状态判断**：匹配页预约/跨校按钮改用内存中的 quota 状态判断，不再解析界面文本；破冰循环变量 `t` 与翻译函数重名一并修复
- **Fixed / 脚本顺序**：`i18n.js` 与 `api()` 帮助函数移入 `<head>`，保证页面内脚本可用
- **已知限制**：后端生成的相处说明书/破冰话题/算法说明及邮件仍为中文（界面已标注）

---

## [1.5.1] — 2026-07-28

### 问题（上次）
- 1.5.0 的深空暗色风对恋爱产品太幽暗压抑，缺少校园恋爱的松弛感
- 背景没有地域标识；支持学校还挂着上海高校（当前不开放）；学校人数徽标不醒目

### 改动
- **Improved / 主题重调**：整体改为「清晨湾区 · 轻科幻」浅色主题——粉蓝渐变底 + 柔光晕 + 白玻璃卡片，保留渐变按钮与网格等科技元素但不再压抑
- **Added / 大湾区天际线**：页面底部固定 SVG 剪影（澳门塔、新葡京、大三巴、港珠澳大桥、广州塔、香港 IFC/中银/ICC），淡靛蓝低透明度不抢内容
- **Removed / 上海高校**：`SCHOOL_DOMAINS` 移除上海交大/复旦/同济/华东师大，只保留澳门五校（香港注释待开放）
- **Improved / 学校列表**：改为按已验证人数降序的胶囊徽标，每个前面带闪烁绿点（呼吸发光），人数加粗高亮
- **Improved**：匹配页/问卷页文字色适配浅色底；首页文案调整为更松弛的恋爱语气

---

## [1.5.0] — 2026-07-28

### 问题（上次）
- 旧 UI 是朴素浅色表单风，对比 CampusDate 等竞品明显「不够想点进来」；落地页也没有讲清产品卖点
- 部分模板内联浅色样式（浅蓝/浅粉面板、灰字）没有统一设计系统，暗色下会看不清

### 改动
- **Added / 深空科技风设计系统**：暗色底 + 动态光晕（orb 漂移动画）+ 网格遮罩 + 玻璃拟态卡片 + 渐变霓虹按钮/文字（自研风格，未复用竞品视觉素材）
- **Added / 新落地页**：渐变大标题 Hero、实时人数徽章、三步流程卡、四特性卡、CTA——信息架构借鉴主流校园匹配站，文案与视觉全部原创
- **Improved / 全站暗色适配**：匹配中心、问卷、验证页的内联浅色样式改为设计系统类（box-info/box-ice/t-ok 等）；匹配度条改为渐变发光 + 生长动画；Likert/多选/重要标记按钮带霓虹选中态
- **Fixed**：README 过时的「32题」描述更新为 40 题五维度；`prefers-reduced-motion` 下自动关闭所有动画

---

## [1.4.1] — 2026-07-28

### 问题（上次）
- 功能已齐，但缺少一份可执行的熟人公测上线清单与推荐配置，容易漏掉邮件/密钥/种子污染

### 改动
- **Added**：`docs/beta-launch.md` — 公测 Checklist、推荐 `.env`、冒烟步骤、一周盯盘指标
- **Docs**：`.env.example` 增加公测推荐档注释；路线图链到公测文档

---

## [1.4.0] — 2026-07-28

### 问题（上次）
- P1 缺口：不能跨校、问卷缺消费/定居/约会节奏等题、熟人无法「不想匹配」
- 黑名单若只填姓名字符串易撞名误伤

### 改动
- **Added / 跨校**：`allow_cross_school` + `CROSS_SCHOOL_MATCHING_ENABLED`；即时与批量都支持跨校池（双方都开才配）
- **Added / 问卷**：Q33–Q40 相处预期（消费、定居、约会频率、恋爱节奏、异地恋、宠物、社交、家务）；异地恋为一票否决
- **Added / 黑名单**：`blocklist` 表；搜昵称/邮箱点选确认；配对卡「不再匹配此人」；双向过滤
- **Improved**：向量维数不一致不再用 zip 截断误算；种子刷新含新题与跨校演示（Alice↔Ivy）

---

## [1.3.0] — 2026-07-28

### 问题（上次）
- 竞品调研显示：用户更在意「匹配理由 / 仪式感 / 配上后怎么开口」，而不是算法题量
- 我们结果页只有短标签，缺少相处说明书与破冰；也缺少「预约本周 → 周二揭晓」仪式

### 改动
- **Added / 相处说明书**：`summary` + 更完整共同点/差异文案写入 insight
- **Added / 破冰话题**：基于共同兴趣与相近量表题自动生成 3 条；页面与邮件展示
- **Added / 本周预约**：`opt_in_week` + `POST/DELETE /api/match/opt-in`；倒计时到揭晓
- **Added / 揭晓模式配置**：`REVEAL_REQUIRE_OPT_IN`（批量只匹配已预约）、`INSTANT_MATCH_ENABLED`（冷启动提前揭晓）
- **Improved**：旧配对加载时自动补全说明书与破冰并回写

---

## [1.2.3] — 2026-07-28

### 竞品深度调研

全面调研 CampusDate (交大)、MatchUs (浙大)、FDU Date (复旦)、Solmate (复旦)、哈啰 Match 的产品、问卷、算法、商业模式和用户反馈。

#### 关键发现
- **算法精度不是护城河**：65题 vs 15维 → 最终恋爱转化率都是 ~6%
- **核心产品价值** = 降低社交启动成本 + 制造仪式感 + 提供"被看见"的体验
- **匹配理由是产品的灵魂** — 竞品用户最高频好评词
- **固定揭晓时间 > 随时可刷** — 每周二晚9点是竞品共同选择
- **MatchUs ¥19.9/次是唯一跑通的付费模型**，但仍是亏本运营

#### 改进方向
- P0: 匹配理由展示、破冰话题、学校认证徽章
- P1: 扩至50题、每周二揭晓页面、跨校匹配
- P2: 匹配后反馈、分享裂变、AI红娘

#### 差异化定位
**大湾区唯一跨境校园恋爱匹配平台** — 竞品都在内地高校，港澳是空白。

#### 文档
- `docs/competitor-research.md`：完整竞品调研报告（问卷设计、商业模式、用户反馈、扩展路径）

---

## [1.2.2] — 2026-07-28

### 问题（上次）
- 一对一配对成功后，页面「历史」仍展示旧 Top-5 留下的其他人姓名与微信号，隐私与产品逻辑都不对

### 改动
- **Fixed**：`Match.active` 标记；新配对会把该用户其余配对降为失效
- **Fixed**：`/api/matches` 默认只返回有效配对；失效记录不返回微信号
- **Migration**：启动时清理每人多条有效配对，只留最近/最高分的一条
- **UI**：按钮改为「当前配对」，文案明确只展示一人

---

## [1.2.1] — 2026-07-28

### 问题（上次）
- 默认 `realtime` Top-5，点一次匹配会出好几个对象，和「一对一」产品预期不符
- 匹配邮件：失败仍标记 `notified=true`，且「更新已有配对」不发信，导致真实用户收不到通知
- 开发者难以理解余弦 / 匈牙利各自用在什么场景

### 改动
- **Fixed / 默认一对一**：`MATCH_MODE=one_to_one`，点匹配只返回得分最高的 1 人；每周额度默认改为 1
- **Fixed / 邮件通知**：每次成功配对会尝试给你发信；只有发到你邮箱成功才标 `notified`；页面展示「发给你/对方」成功或失败原因
- **Added / 匹配说明**：匹配页可展开「匹配是怎么算的」通俗说明（含匈牙利一句话解释）
- **Note**：演示账号如 `alice@um.edu.mo` 不是真实邮箱，发给对方常失败属正常；你自己的学校邮箱应能收到

---

## [1.2.0] — 2026-07-28

### 问题（上次）
- 可无限点「开始匹配」，缺少运营节奏（额度/冷却）
- 批量匹配只是配置项，没有可执行的周二任务与手动触发入口
- 首页看不到各校人数；历史页差异信息不足；邮件失败时用户容易以为「没结果」

### 改动
- **Added / 运营额度**：每用户每周新建匹配上限（默认 3）+ 冷却（默认 12h）；`/api/match/status` 供页面展示
- **Added / 批量任务**：`batch_job.py`（`--now` / `--schedule`）、可选进程内调度、`POST /api/admin/batch-run`
- **Improved / 匹配页**：额度与下次批量时间、共同点+差异、邮件失败仍展示结果提示
- **Improved / 首页**：各校已验证人数 + 总人数 + 批量匹配时间说明
- **Docs**：更新 `docs/mvp-roadmap.md`、`.env.example`
- **Chore**：清理 `app.py` 未使用导入

---

## [1.1.0] — 2026-07-28

### 问题（上次）
- 匹配写死「男只找女 / 女只找男」，无法支持同性或双性取向
- 上线前缺少生产开关、验证码限流、隐私说明与健康检查，不敢直接给同学用

### 改动
- **Added / 择偶取向**：问卷基本信息增加「希望匹配的性别」（男 / 女 / 男女都可以）；匹配需双方取向互相接受
- **Fixed / 匹配引擎**：`orientation_compatible` 双向过滤；批量模式在含同性/不限时改用贪心配对
- **Added / MVP 1.1 上线项**：`FLASK_DEBUG`、Session 安全、注册限流、`/api/health`、首页隐私说明
- **Fixed / 注册**：SMTP 失败时仍返回验证码到页面，不阻断验证流程
- **Docs**：`docs/mvp-roadmap.md`；`.env.example` 补充生产配置项
- **Seed**：默认异性取向；Emma 设为 `both` 便于演示

---

## [1.0.1] — 2026-07-28

### 问题（上次）
- 问卷量表滑块默认停在 3，进度却显示未答/提交又偷偷带上默认值，交互混乱
- `answers` JSON 键是字符串、匹配逻辑用 int 取，导致一票否决和匹配理由实际失效
- 「重新匹配」遇到已有记录直接跳过，页面像匹配失败；旧逻辑还可能重复发邮件
- 种子用户兴趣选项写错（咖啡馆/民谣等），特征向量对不上题库

### 改动
- **Fixed / 问卷页**：改为 1–5 点选；未点不算已答；提交前校验并高亮缺题
- **Fixed / 数据层**：`answers`、`important_qids` 读写统一为 int 键
- **Fixed / 匹配**：更新已有 Match 并返回结果；邮件只通知新匹配
- **Fixed / 种子与 API**：选项纠错 + `seed.py --refresh`；不足 20 题拒绝提交；过滤非法 multi
- **Improved**：进度 sticky、已答/未答状态、★ 重要标记、移动端 Likert；共同点含兴趣交集
- **Docs**：约定每次推送前必须更新本更改日志（见 `.cursor/rules/changelog-on-push.mdc`）

---

## [1.0.0] — 2026-07-27

### MVP 首个完整可用版本

#### Added
- **学校邮箱注册 + QQ SMTP 真实邮件验证**
  - 域名白名单自动识别学校
  - QQ 邮箱 SMTP 发送 6 位验证码
  - 10 分钟过期机制
  - 开发模式 fallback：邮件失败时控制台打印 token

- **32题深度问卷系统**
  - 四维度：核心价值观(8) / 生活习惯(8) / 情感风格(8) / 兴趣爱好(8)
  - Scale 滑块 (1-5) + Multi 多选
  - "对我很重要" 标记（权重 ×2）
  - 一票否决（出轨/孩子/婚姻/抽烟）

- **81维特征向量 + 余弦相似度匹配**
  - 问卷答案 → 归一化特征向量
  - 余弦相似度 Top-N 实时匹配
  - 匈牙利算法全局最优匹配（批量模式备用）
  - 一票否决过滤

- **匹配结果邮件通知**
  - 双方收到对方微信号 + 匹配度百分比
  - 共同点 + 差异分析
  - 样式化 HTML 邮件

- **种子数据生成器**
  - 澳大 8人（4男4女）+ 澳科 1人
  - 差异化人设：心理学/工科/法律/商科/传理/教育/金融/计算机/设计
  - 81维特征向量预计算

- **公网部署**
  - serveo SSH 反向隧道，零成本
  - pythonw 后台运行，不依赖终端
  - .env 环境变量管理

- **爬虫策略模块**
  - 多源搜索策略生成
  - 学校兴趣标签自动提取
  - 日报系统同款 pipeline

#### Supported Schools
- 澳门大学 (um.edu.mo)
- 澳门科技大学 (must.edu.mo)
- 澳门理工大学 (mpu.edu.mo)
- 澳门旅游大学 (iftm.edu.mo)
- 澳门城市大学 (cityu.edu.mo)
- 上海交通大学 (sjtu.edu.cn) — 参考实现
- 复旦大学 (fudan.edu.cn)
- 同济大学 (tongji.edu.cn)
- 华东师范大学 (ecnu.edu.cn)

#### Tech Stack
- Python 3 / Flask / SQLAlchemy / SQLite
- Jinja2 / Vanilla JS / CSS
- QQ SMTP / serveo SSH tunnel

---

## [0.1.0] — 2026-07-27 (early)

### 初始原型

- Flask 骨架 + SQLite
- 标签关键词匹配（简单交集）
- 上海高校邮箱白名单
- 开发模式邮件打印

---

### 版本命名规则

```
v<MAJOR>.<MINOR>.<PATCH>

MAJOR: 不兼容的架构变更
MINOR: 新功能、新学校、新算法
PATCH: Bug 修复、样式调整
```
